"use client";

import CompUpload from "public/assets/icon/upload.svg"

const IconUpload = (props : {className?:string; style?:React.CSSProperties}) => {
    return <CompUpload {...props}/>
}

export default IconUpload