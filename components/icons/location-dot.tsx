"use client";

import CompLocationDot from "public/assets/icon/location-dot.svg"

const IconLocationDot = (props : {className?:string; style?:React.CSSProperties}) => {
    return <CompLocationDot {...props}/>
}

export default IconLocationDot