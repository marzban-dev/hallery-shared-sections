"use client";

import CompPalette from "public/assets/icon/palette.svg"

const IconPalette = (props : {className?:string; style?:React.CSSProperties}) => {
    return <CompPalette {...props}/>
}

export default IconPalette