"use client";

import CompLinkStroke from "shared/components/icons/svgs/link-stroke.svg"

const IconLinkStroke = (props : {className?:string; style?:React.CSSProperties}) => {
    return <CompLinkStroke {...props}/>
}

export default IconLinkStroke