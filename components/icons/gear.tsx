"use client";

import CompGear from "shared/components/icons/svgs/gear.svg"

const IconGear = (props : {className?:string; style?:React.CSSProperties}) => {
    return <CompGear {...props}/>
}

export default IconGear