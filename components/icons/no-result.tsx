"use client";

import CompNoResult from "shared/components/icons/svgs/no-result.svg"

const IconNoResult = (props : {className?:string; style?:React.CSSProperties}) => {
    return <CompNoResult {...props}/>
}

export default IconNoResult