"use client";

import CompCircleCheck from "shared/components/icons/svgs/circle-check.svg"

const IconCircleCheck = (props : {className?:string; style?:React.CSSProperties}) => {
    return <CompCircleCheck {...props}/>
}

export default IconCircleCheck