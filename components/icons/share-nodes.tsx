"use client";

import CompShareNodes from "shared/components/icons/svgs/share-nodes.svg"

const IconShareNodes = (props : {className?:string; style?:React.CSSProperties}) => {
    return <CompShareNodes {...props}/>
}

export default IconShareNodes