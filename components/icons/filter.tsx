"use client";

import CompFilter from "public/assets/icon/filter.svg"

const IconFilter = (props : {className?:string; style?:React.CSSProperties}) => {
    return <CompFilter {...props}/>
}

export default IconFilter