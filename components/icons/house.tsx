"use client";

import CompHouse from "public/assets/icon/house.svg"

const IconHouse = (props : {className?:string; style?:React.CSSProperties}) => {
    return <CompHouse {...props}/>
}

export default IconHouse