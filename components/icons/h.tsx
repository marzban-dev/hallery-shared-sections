"use client";

import CompH from "shared/components/icons/svgs/h.svg"

const IconH = (props : {className?:string; style?:React.CSSProperties}) => {
    return <CompH {...props}/>
}

export default IconH