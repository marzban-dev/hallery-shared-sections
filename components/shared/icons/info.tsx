"use client";

import CompInfo from "public/assets/icon/info.svg"

const IconInfo = (props : {className?:string; style?:React.CSSProperties}) => {
    return <CompInfo {...props}/>
}

export default IconInfo